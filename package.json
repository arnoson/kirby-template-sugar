{
  "name": "@kirby-template-sugar/monorepo",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "check-types": "tsc --noEmit",
    "release": "bumpp -r"
  },
  "devDependencies": {
    "bumpp": "^9.1.1"
  }
}
