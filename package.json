{
  "name": "@kirby-template-sugar/monorepo",
  "version": "0.3.2",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm --filter \"./packages/**\" build",
    "test": "pnpm -r test",
    "check-types": "tsc --noEmit",
    "release": "bumpp -r"
  },
  "devDependencies": {
    "bumpp": "^10.0.3"
  }
}
