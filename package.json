{
  "name": "@kirby-template-sugar/monorepo",
  "version": "0.4.3",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm --filter \"./packages/**\" build",
    "test": "pnpm -r test",
    "check-types": "tsc --noEmit",
    "release": "bumpp -r"
  },
  "devDependencies": {
    "bumpp": "^10.2.0",
    "typescript": "^5.8.3"
  }
}
