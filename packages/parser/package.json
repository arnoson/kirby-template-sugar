{
  "name": "@kirby-template-sugar/parser",
  "type": "module",
  "version": "0.1.3",
  "license": "MIT",
  "scripts": {
    "build": "tsup ./src/parser.ts --clean --dts --format esm --target esnext",
    "test": "vitest run",
    "test:dev": "vitest watch"
  },
  "dependencies": {
    "tsx": "^3.12.7"
  },
  "devDependencies": {
    "vitest": "^0.33.0"
  }
}
